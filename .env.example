# PWA cloud API uses the fixed endpoint recovered from the Android source.
# No cloud secrets are required in environment files.
# Local bridge configuration is passed as process environment variables:
# BRIDGE_PORT=8788
# PWA_ORIGINS=http://localhost:3000,https://heyanush.github.io
# BRIDGE_KEY is optional; prefer the random pairing key generated at startup.
